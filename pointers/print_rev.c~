#include <stdio.h>
#include <string.h>
int main()
{
  char mystr[60];
  int len,i;
  printf("Enter string you want to be reversed: ");
  scanf("%c",mystr);
  len = strlen(mystring);
  for(i=len-1;i>=0;i--){
    putchar(mystring[i]);
  }
  return 0;
}
