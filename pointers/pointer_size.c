
#include <stdio.h>

int main()
{
  int *p;
  char *c;
  printf("The size of a integer pointer variable is %lu \n", sizeof(p));
  printf("The size of a character pointer variable is %lu \n",sizeof(c));
  return 0;
}
